import { FieldResizingDirective } from './field-resizing.directive';

describe('FieldResizingDirective', () => {
  it('should create an instance', () => {
    const directive = new FieldResizingDirective();
    expect(directive).toBeTruthy();
  });
});
